package tech.zeroed.doover.gameobjects.ai;

import tech.zeroed.doover.gameobjects.enemy.Enemy;

public class MoveToPosition extends AI {
    public MoveToPosition(Enemy target) {
        super(target);
    }

    @Override
    public void update(float delta) {

    }
}
